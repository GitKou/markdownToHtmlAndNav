{
  "name": "markdown-to-nav",
  "version": "1.0.4",
  "description": "Transfer a markdown file to a html file and output its nav",
  "main": "marked.js",
  "bin": {
    "mdToNav": "./bin/mdToNav"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "gmm",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "https://github.com/GitKou/markdownToHtmlAndNav.git"
  },
  "devDependencies": {
    "cheerio": "^1.0.0-rc.1",
    "commander": "^2.9.0",
    "marked": "^0.3.6"
  }
}
